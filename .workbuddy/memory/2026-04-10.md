# 2026-04-10

## UI 设计方案 v4.0 (r2 修订版)
- 文件: `specs/ui-redesign-v4/design-spec.md`
- r1 → r2 修订要点（根据用户反馈）:
  - 废弃手势盘(quick-dial)，快捷记录保留一键直达弹窗
  - 状态胶囊承载睡眠开始/结束 + 自动同步写入记录
  - 全文禁止 emoji，统一使用已有 PNG 图标 + Iconify 补充
  - 去掉 stat-ring/quick-dial 两个新组件，仅新增 focus-card 1 个
  - 整体进一步精简，工时从 9-14 天缩减到 5-8 天
- 核心变更：
  - 首页从 10+ 区域精简到 7 个区域
  - 状态胶囊: 睡眠中 → 点击结束 → 自动调 RecordService 写入记录
  - 摘要卡片: 数字从 40rpx 加大到 48rpx + 新增进度条
  - 待办: 横向滚动改为竖向堆叠条
  - AI 洞察: 改为一行式摘要 + 展开
  - 时间线: 新增左侧时间轴线 + 功能色节点
  - 发现页: 功能入口改 2x2 网格 + 聚焦卡片
  - 我的页: 用户信息居中 + 菜单去描述精简
- 仅新增 6 个 CSS 变量 + 3 个 Iconify 补充图标（sun/moon/rec-dot）

## 全页面/组件细化规格
- 文件: `specs/ui-redesign-v4/detailed-spec.md`
- 覆盖全部 18 个页面 + 15 个组件的逐一细化
- 定义了 4 套全局统一规范: 页头样式、弹窗规范、表单规范、空状态规范
- 所有居中弹窗统一改为底部弹出（vaccine详情、milestone详情、growth WHO标准、family权限/转让等）
- 暗色模式逐项检查矩阵覆盖 29 个文件
- settings 页面主题选择去除文字字符(emoji)，改为 Iconify PNG 图标

## 三轮代码交叉审查修复
- 对 design-spec.md + detailed-spec.md 结合代码做了三轮审查
- 修复问题:
  1. Iconify 图标数量不一致(4→3)，design-spec 路线图和审查清单已同步
  2. 弹窗规范 v3.x 基线值全部校准(popup.wxss 实际是 80rpx/8rpx 指示条、64rpx 图标容器、36rpx 标题)
  3. 表单规范 v3.x 基线值校准(form.wxss 实际是 32rpx 图标、88rpx 输入框、96rpx 按钮)
  4. 补充 15 个居中弹窗的改底部弹出清单(含 discover 参考弹窗、profile 编辑弹窗、auth 邀请码弹窗等)
  5. 补充 4 个共享样式文件(popup/page-header/form/loading.wxss)的影响范围说明
  6. 补充 profile 编辑弹窗从居中改底部弹出的规格
  7. 精确标注 settings 页面哪些是 Unicode 文字符号(☀/⚙)、哪些是 emoji(🌙)
  8. 明确 14 个去掉页头图标的页面列表 + 1 个保留的页面(discover)
